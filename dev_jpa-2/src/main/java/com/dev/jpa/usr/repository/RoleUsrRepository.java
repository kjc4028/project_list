package com.dev.jpa.usr.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.dev.jpa.usr.entity.RoleUsrEntity;

public interface RoleUsrRepository extends JpaRepository<RoleUsrEntity, Long>{
	
}
